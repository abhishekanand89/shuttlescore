# Project Manifest

> Auto-generated by the multi-agent system. Updated as features change the architecture.

## Project
- **Name**: Badminton Scorer
- **Description**: Mobile-first badminton match scorer — tracks point-by-point scoring for singles and doubles, with player identification and tournament tagging
- **Repository**: local

## Tech Stack
- **Runtime**: Python 3.12 (backend), Node.js 20 (frontend)
- **Language**: Python (backend), TypeScript (frontend)
- **Framework**: FastAPI 0.115+ (backend), React 19 + Vite 6 (frontend PWA)
- **Database**: SQLite + SQLAlchemy 2.0 (async) + Alembic (migrations)
- **Auth**: Simple player identification (no heavy auth for MVP)
- **Testing**: pytest + httpx (backend), Vitest + Testing Library (frontend)
- **Styling**: Vanilla CSS (mobile-first, responsive)
- **Package Manager**: pip + venv (backend), npm (frontend)

## Architecture Summary
- `backend/` — FastAPI application
  - `backend/app/` — Application package
  - `backend/app/api/` — API route handlers
  - `backend/app/models/` — SQLAlchemy ORM models
  - `backend/app/schemas/` — Pydantic request/response schemas
  - `backend/app/services/` — Business logic layer
  - `backend/app/db/` — Database configuration and migrations
- `frontend/` — React + Vite PWA
  - `frontend/src/components/` — React components
  - `frontend/src/pages/` — Page-level components
  - `frontend/src/hooks/` — Custom React hooks
  - `frontend/src/api/` — API client / fetch wrappers
  - `frontend/src/context/` — React context providers
- `tests/` — Test files
  - `tests/unit/` — Unit tests
  - `tests/api/` — API endpoint tests
  - `tests/e2e/` — End-to-end tests

## Key Abstractions
- API Response Format: `{ "success": true, "data": T }` or `{ "success": false, "error": "message" }`
- Component Pattern: Functional components with hooks, mobile-first
- Data Fetching: Custom `useApi` hook wrapping fetch
- Error Handling: FastAPI exception handlers → consistent error responses
- Scoring Engine: Centralized service that enforces badminton rules (21-point, deuce, 30-cap)

## Existing Test Patterns
- **Framework**: pytest (backend), Vitest (frontend)
- **Location**: `tests/`
- **Naming Convention**: `test_*.py` (backend), `*.test.tsx` (frontend)
- **Run Command**: `cd backend && pytest` / `cd frontend && npm test`
- **Example File**: (none yet — first feature will establish patterns)

## Key Dependencies
| Dependency | Version | Purpose |
|-----------|---------|---------|
| fastapi | 0.115+ | Web framework |
| sqlalchemy | 2.0+ | ORM (async) |
| alembic | 1.13+ | Database migrations |
| pydantic | 2.0+ | Request/response validation |
| uvicorn | 0.30+ | ASGI server |
| react | 19 | UI framework |
| vite | 6 | Build tool + dev server |
| vite-plugin-pwa | 0.20+ | PWA support (installable on mobile) |

## Feature History
| ID | Name | Date Completed |
|----|------|---------------|
| — | — | — |
